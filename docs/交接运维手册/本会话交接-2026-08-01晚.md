# AooBee 本会话交接文档（2026-08-01 晚）

> **用途**：本会话（2026-08-01 晚）对仓库做了若干约定变更与收口。新会话开读 `AGENTS.md` + `docs/交接运维手册/整体交接运维手册.md` + **本文件** 即可无缝接手，无需重翻历史。
> **当前 git**：`main` 已对齐 `origin/main`（HEAD `32bd65b`），无未推送 commit；唯一未跟踪 `.next_stale/`（构建残留，不提交）。

---

## 〇、最重要的一句话（必读，先看这条）

1. **AI 现在可以直接 `git push`** —— 本沙箱实测成功，**旧"AI 只 commit 不 push"铁律已废弃**。改完文件 `commit` 后**直接 `push`**，不用等用户手动推。
2. **但三步法仍 100% 生效** —— 任何文件改动（代码 / 文档 / 配置 / 记忆文件）仍须 **先给方案 → 等用户确认 → 再落盘**。"能 push" ≠ "能擅自改文件"，两件事分开看。
3. **给用户的命令仍用 `;`**（PowerShell 不支持 `&&`）；不要替用户虚构紧迫感、不要做命令之外的额外适配。

---

## 一、本会话做了什么（升级改造清单）

### 1.1 Git 推送约定变革（核心，覆盖旧铁律）
- **旧约定（已废弃）**：AI 只 `commit` 不 `push`，理由"HTTPS + Git Credential Manager 无 /dev/tty，push 必失败"。
- **实测（2026-08-01）**：直接 `git push origin main` **成功**（输出 `af5de34..e391117` 类），证明沙箱现存在可用凭据机制，旧假设不成立。
- **新约定**：**AI 直接 `commit` 并 `push`**。
- **已同步文档（三处，均 commit + push）**：
  - `AGENTS.md` §2 第4步 / §3 Git 工作流 / §6 第5条 → commit `617f8f1`
  - `.workbuddy/memory/MEMORY.md`「Git 推送约定」段 → 随 `617f8f1`
  - `docs/交接运维手册/整体交接运维手册.md` §0/§4.1/§4.3/§7.2/§7.6/§8.3/§10.3/附录（共 10 处）→ commit `af5de34`
- **保留不变**：`PowerShell 不支持 &&`（给用户的命令用 `;`）仍有效，与 push 无关。

### 1.2 Obsidian 单 Vault 确认
- 用户确认：**维持现状** —— Obsidian 直接打开 `content/` 文件夹作为单一 Vault，**不另设独立 Vault**。
- 含义：编辑即改源文件，无同步工具、无双向同步；`[[双链]]` 仅个人知识图谱、**不上站**，站内互链必须写 `related:` 字段。

### 1.3 收口与推送（本会话已完成）
- 收口 commit `e391117`（2026-08-01 工作日志）。
- 已 push 的关键 commit：`dc5b382`（产品页编写规矩）`e391117`（日志收口）`617f8f1`（push 约定-AGENTS/MEMORY）`af5de34`（push 约定-交接手册）`32bd65b`（本会话日志追加）。
- Vercel 已随每次 push 自动重建部署（docs 改动也触发一次，无害）。

---

## 二、当前 git 状态（2026-08-01 晚）

- `main` == `origin/main` == `32bd65b`，无 ahead / 无 behind。
- 唯一未跟踪：`.next_stale/`（构建残留，非仓库文件，本地可删，不提交）。
- 近期关键 commit 链：
  ```
  32bd65b  chore(memory): 记录 Git 推送约定更正（本会话日志追加）
  af5de34  docs(handover): 同步更正 Git 推送约定——本沙箱可正常 push
  617f8f1  docs(git): 更正 Git 推送约定——本沙箱可正常 push，AI 可直接 push
  e391117  chore(memory): 收口 2026-08-01 工作日志
  dc5b382  docs: 产品页编写规矩写入 AGENTS/运营手册
  ```

---

## 三、新会话接手步骤

1. 读 `AGENTS.md`（AI 运营总纲）+ `docs/交接运维手册/整体交接运维手册.md`（伞形总纲）+ **本文件**。
2. `git status -sb` 核对（应与 `origin/main` 对齐，仅 `.next_stale/` 未跟踪）。
3. 任何文件改动：**先给方案 → 等确认 → 再落盘**（三步法，最高优先级）。
4. 改完 `git add <文件>` + `git commit -m "描述"` + `git push origin main`（push 现已可用，直接推）。
5. 线上验证：Vercel 自动重建（约 1–2 分钟）；docs 改动也触发部署，无害。

---

## 四、主线待办（按优先级）

- **P0 · GSC 催收录**：用 `docs/GSC索引提交清单.md`（66 URL）在 Search Console → URL 检查逐个「请求编入索引」，每日有额度、分批做，**优先 49 篇扩写内容**（Batch1 9 + Batch2 15 + Batch3 25）。新域名收录慢属正常，别焦虑。
- **P1 · sameAs 真实社媒 URL**：用户给链接后，AI 填入 `src/lib/seo/schema.ts` 的 `organizationSchema.sameAs`（当前占位 `["https://www.aoobee.com","/about","/contact"]`）。
- **被动 · 勿动**：
  - AdSense 审核（老账号 `ca-pub-8752263153695128` 添加站点后等审核，不主动运营广告位）；
  - X 申诉（OG 图曾标记「潜在有害」，已回退原生 OG + 提交申诉，回执后 2–4 天回复，期间**勿重复提交**）。
- **可选 · 用户拍板**：GEO 度量闭环（隐私友好 analytics + AI 爬虫日志，当前最大 ROI 盲区）、外链建设（新域名零外链、权威弱）、内容扩量（~300 节点内无压力）。

---

## 五、内容约定速记（易踩坑，详见 `AGENTS.md` §1）

- `content/reviews/`（用户点评）≠ `content/articles/review/`（编辑评测）—— **两套内容，绝不可混放**。
- **产品页正文**：按空行切成**纯文本段落**渲染、**不解析 Markdown**；`#`/`##`/`**` 会原样显示成文字；所有问答**只写进 `faqItems`**，别写进正文。
- 分类 8 选 1（`zhuangxiu/jiadian/yuer/weixiu/bangong/yanglao/jiaoyu/ai`）；`related:` 写路径，解析失败**静默过滤不报错**（不产生死链）。
- FAQ 写进 `faqItems` 自动生成 FAQPage schema；站点结构化数据 / `llms.txt` / `sitemap.xml` / `robots.txt` / OG 图均**构建期自动生成，勿手改**。

---

## 六、关键文件索引

| 文件 | 用途 |
|---|---|
| `AGENTS.md` | AI 运营总纲（新会话必读，push 约定已更正） |
| `docs/交接运维手册/整体交接运维手册.md` | 全套接手手册（伞形总纲，push 约定已更正） |
| **本文件** | 本会话交接（本次新增，浓缩变更 + 接手步骤） |
| `docs/GSC索引提交清单.md` | 66 URL 催收录清单（P0 用） |
| `docs/分析报告/网站SEO-GEO-架构运维分析报告-2026-08-01.md` | 五维评分（技术 92 / 内容 55 / 索引 30 / GEO 90 / 架构 85）与行动清单 |
| `.workbuddy/memory/MEMORY.md` | 长期约定（Git 推送已更正为可 push） |
| `.workbuddy/memory/2026-08-01.md` | 本会话流水细节 |

---

*本文件为「本会话 → 新会话」的接手桥，随项目演进而更新；约定变更请同步 `AGENTS.md` 与 `整体交接运维手册.md`。*
