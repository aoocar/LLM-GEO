main:
  push:
    - name: build-verify
      docker:
        image: node:22
      stages:
        - name: install-deps
          script: npm ci
        - name: build
          script: npm run build
    - name: sync-to-github
      stages:
        - name: push-to-github
          script: |
            echo "=== 同步 CNB main 到 GitHub aoocar/LLM-GEO ==="
            # 从密钥仓库读取 GitHub token（不回显 token 值）
            export GH_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" -H "Accept: application/json" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/LLM-GEO")"
            if [ -z "$GH_TOKEN" ]; then
              echo "ERROR: GitHub token 读取失败"
              exit 1
            fi
            echo "Token loaded, length=${#GH_TOKEN}"
            # 确保 main 分支存在且最新
            git checkout main 2>/dev/null || true
            # 配置 git 身份
            git config user.name "cnb.c4ejfLqtAfAA"
            git config user.email "XlETCNISXrwdXD66uicfzE+cnb.c4ejfLqtAfAA@noreply.cnb.cool"
            # 配置 GitHub remote（token 隐藏）
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${GH_TOKEN}@github.com/aoocar/LLM-GEO.git"
            # 推送 main 到 GitHub
            echo "--- 推送前 GitHub HEAD ---"
            git ls-remote github HEAD || true
            echo "--- 开始推送 ---"
            git push github "main:refs/heads/main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true
            echo "--- 推送后 GitHub HEAD ---"
            git ls-remote github HEAD || true
            echo "=== 同步完成 ==="
  pull_request:
    - name: pr-build-verify
      docker:
        image: node:22
      stages:
        - name: install-deps
          script: npm ci
        - name: build
          script: npm run build
        - name: lint
          script: npm run lint
  # 每日定时拉取 GSC/GA4 数据并生成 SEO 报告
  "crontab: 30 6 * * *":
    - name: fetch-seo-report
      docker:
        image: node:22
      stages:
        - name: fetch-and-push
          script: |
            echo "=== [SEO拉数] 读取 Google 服务账号密钥 ==="
            curl -s -o /tmp/sa.json -H "Authorization: Bearer $CNB_TOKEN" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/gen-lang-client-0803005687-0f2590780ae7.json"
            if [ ! -s /tmp/sa.json ]; then
              echo "ERROR: Google 服务账号密钥读取失败"
              exit 1
            fi
            echo "密钥已读取，大小: $(wc -c < /tmp/sa.json) 字节"

            echo ""
            echo "=== 运行 GSC + GA4 拉数脚本 ==="
            export GOOGLE_SA_FILE=/tmp/sa.json
            node scripts/fetch-seo-data.mjs || echo "⚠️ 拉数脚本异常退出（数据不足或 API 暂时失败）"

            if [ ! -d docs/seo-reports ]; then
              echo "没有报告目录，无需提交"
              exit 0
            fi
            REPORT_FILE=$(ls -t docs/seo-reports/seo-*.md 2>/dev/null | head -1 || true)
            if [ -z "$REPORT_FILE" ]; then
              echo "没有报告文件，无需提交"
              exit 0
            fi
            echo "最新报告: $REPORT_FILE"

            git add docs/seo-reports/
            if git diff --cached --quiet; then
              echo "报告无变更（今天已生成过），无需提交"
              exit 0
            fi

            echo ""
            echo "=== 提交报告 ==="
            git config user.name "AooBee SEO Bot"
            git config user.email "seo-bot@aoobee.com"
            git commit -m "docs(seo): 自动 SEO 数据报告更新 [ci skip]"

            echo "=== 1/2 推送到 CNB（[ci skip] 防死循环）==="
            git push origin "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true

            echo "=== 2/2 推送到 GitHub（触发 Vercel 部署）==="
            export GH_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" -H "Accept: application/json" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/LLM-GEO")"
            if [ -z "$GH_TOKEN" ]; then
              echo "ERROR: GitHub token 读取失败"
              exit 1
            fi
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${GH_TOKEN}@github.com/aoocar/LLM-GEO.git"
            git push github "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true
            echo "=== 推送完成 ==="

  # 每日 07:00 拉取 Microsoft Clarity 流量数据并生成日报（真人/爬虫、行为质量、画像、热门页面、404 检测）
  # token 从 aoobee-secrets 密钥仓库的 aoobee-seo-bot 文件注入，不写明文、不打日志
  # 配额：Clarity Data Export API 每日 10 次，本任务每日 2 次调用（近 3 天窗口），远低于上限
  "crontab: 0 7 * * *":
    - name: fetch-clarity-report
      docker:
        image: node:22
      stages:
        - name: fetch-and-push
          script: |
            echo "=== [Clarity拉数] 读取 Clarity API token（aoobee-seo-bot）==="
            CLARITY_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/aoobee-seo-bot")"
            if [ -z "$CLARITY_TOKEN" ]; then
              echo "ERROR: Clarity token 读取失败（请确认 aoobee-secrets/aoobee-seo-bot 存在）"
              exit 1
            fi
            echo "token 已读取，长度=${#CLARITY_TOKEN}（不打印内容）"

            echo ""
            echo "=== 运行 Clarity 拉数脚本 ==="
            export CLARITY_TOKEN
            node scripts/fetch-clarity.mjs --days=3 || echo "⚠️ Clarity 拉数脚本异常退出（配额/网络/临时失败），不影响主流程"

            if [ ! -d docs/seo-reports ]; then
              echo "没有报告目录，无需提交"
              exit 0
            fi
            REPORT_FILE=$(ls -t docs/seo-reports/clarity-*.md 2>/dev/null | head -1 || true)
            if [ -z "$REPORT_FILE" ]; then
              echo "没有 Clarity 报告文件，无需提交"
              exit 0
            fi
            echo "最新报告: $REPORT_FILE"

            git add docs/seo-reports/
            if git diff --cached --quiet; then
              echo "报告无变更（今天已生成过），无需提交"
              exit 0
            fi

            echo ""
            echo "=== 提交报告 ==="
            git config user.name "AooBee SEO Bot"
            git config user.email "seo-bot@aoobee.com"
            git commit -m "docs(seo): 自动 Clarity 流量报告更新 [ci skip]"

            echo "=== 1/2 推送到 CNB（[ci skip] 防死循环）==="
            git push origin "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true

            echo "=== 2/2 推送到 GitHub（触发 Vercel 部署）==="
            export GH_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" -H "Accept: application/json" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/LLM-GEO")"
            if [ -z "$GH_TOKEN" ]; then
              echo "ERROR: GitHub token 读取失败"
              exit 1
            fi
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${GH_TOKEN}@github.com/aoocar/LLM-GEO.git"
            git push github "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true
            echo "=== 推送完成 ==="

  # 每日 07:30 向百度普通收录 API 提交 sitemap URL（每日 10 条轮转，配额友好）
  # token 从 aoobee-secrets 密钥仓库的 BAIDU_TOKEN 文件注入，不写明文、不打日志
  "crontab: 30 7 * * *":
    - name: submit-baidu-urls
      docker:
        image: node:22
      stages:
        - name: submit
          script: |
            echo "=== [百度收录] 读取 BAIDU_TOKEN ==="
            BAIDU_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/BAIDU_TOKEN")"
            if [ -z "$BAIDU_TOKEN" ]; then
              echo "ERROR: 百度 token 读取失败（请在 aoobee-secrets 仓库新增 BAIDU_TOKEN 文件）"
              exit 1
            fi
            echo "token 已读取，长度=${#BAIDU_TOKEN}"

            echo "=== 提交 sitemap URL 到百度收录（每日 10 条轮转）==="
            export BAIDU_TOKEN
            node scripts/submit-baidu-urls.mjs --rotate || echo "⚠️ 百度提交异常（配额/网络/临时失败），不影响主流程"
            echo "=== 百度收录提交完成 ==="
  "crontab: 0 8 * * 1":
    - name: refresh-ranking-timeliness
      docker:
        image: node:22
      stages:
        - name: refresh-and-push
          script: |
            echo "=== [榜单时效] 刷新 BEST 榜单 updatedAt ==="
            git config user.name "AooBee SEO Bot"
            git config user.email "seo-bot@aoobee.com"

            # 幂等：仅当 updatedAt 非当天时改写，无变化则 git 无 diff
            node scripts/refresh-ranking-timeliness.mjs

            git add content/articles/best/
            if git diff --cached --quiet; then
              echo "榜单时效均已是最新，无需提交"
              exit 0
            fi

            git commit -m "chore(seo): 刷新 BEST 榜单时效 updatedAt [ci skip]"

            echo "=== 1/2 推送到 CNB（[ci skip] 防死循环）==="
            git push origin "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true

            echo "=== 2/2 推送到 GitHub（触发 Vercel 部署）==="
            export GH_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" -H "Accept: application/json" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/LLM-GEO")"
            if [ -z "$GH_TOKEN" ]; then
              echo "ERROR: GitHub token 读取失败"
              exit 1
            fi
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${GH_TOKEN}@github.com/aoocar/LLM-GEO.git"
            git push github "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true
            echo "=== 榜单时效刷新完成 ==="

  # 每周三 08:30 (Asia/Shanghai) 自动生成"城市+本地生活服务"长尾内容（地域/行业不限）
  "crontab: 30 8 * * 3":
    - name: generate-local-pages
      docker:
        image: node:22
      stages:
        - name: generate-and-push
          script: |
            echo "=== [长尾内容] 自动生成城市本地服务 FAQ 文章 ==="
            git config user.name "AooBee SEO Bot"
            git config user.email "seo-bot@aoobee.com"

            # 幂等：仅生成不存在的 slug，无新内容时 git 无 diff 不产生空提交
            node scripts/generate-local-pages.mjs

            git add content/articles/faq/
            if git diff --cached --quiet; then
              echo "无新增长尾页面，无需提交"
              exit 0
            fi

            git commit -m "content(seo): 新增城市本地服务长尾 FAQ 页面 [ci skip]"

            echo "=== 1/2 推送到 CNB（[ci skip] 防死循环）==="
            git push origin "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true

            echo "=== 2/2 推送到 GitHub（触发 Vercel 部署）==="
            export GH_TOKEN="$(curl -s -H "Authorization: Bearer $CNB_TOKEN" -H "Accept: application/json" "https://api.cnb.cool/aoobee/aoobee-secrets/-/git/raw/main/LLM-GEO")"
            if [ -z "$GH_TOKEN" ]; then
              echo "ERROR: GitHub token 读取失败"
              exit 1
            fi
            git remote remove github 2>/dev/null || true
            git remote add github "https://x-access-token:${GH_TOKEN}@github.com/aoocar/LLM-GEO.git"
            git push github "HEAD:main" 2>&1 | grep -viE 'password|username|hint:|remote: Resolving' || true
            echo "=== 长尾内容生成完成 ==="
