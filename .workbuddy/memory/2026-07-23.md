# 2026-07-23

## 本地预览运行（用户："先在本地跑起来看一下"）
- 项目为静态导出（output:'export'），构建产物在 `out/`。本地查看方式：用 Python http.server 伺服 `out/` 目录。
- 命令（Git Bash 必须用 POSIX 路径，反斜杠会被吃）：
  `cd out && /c/Users/aoobe/.workbuddy/binaries/python/versions/3.13.12/python.exe -m http.server 4173 --bind 127.0.0.1`
- 验证：根路径与 sitemap/robots/llms.txt 直接 200；子页 `/ai` 等 301 重定向到 `/ai/`（目录补斜杠，浏览器自动跟随），带斜杠形式均 200。
- 坑：首次用 `C:\Users\...` 反斜杠路径在 bash 里被转义成 `C:Users...` 导致 command not found；改 POSIX 路径 `/c/Users/...` 后正常。
- 预览地址 http://127.0.0.1:4173 已在后台运行（任务 jQBU3H）。

## nginx 配置待办（关联方案2）
- 已开启 `trailingSlash: true`，Next 现把子页导出为 `out/ai/index.html`、`out/ai/chatgpt/index.html`（目录索引形式）。nginx 部署 `out/` 用 `index index.html; try_files $uri $uri/ =404;` 即可，干净 URL 直接可用，不再有「目录列表」问题。
- 注意：Windows 上后台预览服务（python http.server）会锁定 `out/` 目录，导致 `next build` 收尾清理报 `EBUSY: resource busy or locked, rmdir 'out'`。构建前务必先停掉预览服务（用 `Get-NetTCPConnection -LocalPort 4173` 找 PID 再 `Stop-Process -Force`）。

## 前端 bug 排查与修复（用户："自己浏览查找 bug 并优化"）
- 用 Python 爬虫脚本（/tmp/aoobee_audit2.py）爬全站 25 页，查死链/缺失资源/JSON-LD/title-h1-description。
- 修复的致命 + SEO 缺陷（已提交 dc5f4b8）：
  1. 【致命】`trailingSlash` 未开启 → 静态托管下子页 `/ai` 被当成目录列表打不开。已设为 true。
  2. footer 硬编码 5 个拼音分类 + best 4 话题链接 → 404；改为 getCategories()/getArticlesByType 动态数据。
  3. header/hero 搜索 `action="/search"` → 404；新增 /search 静态搜索页（服务端建索引 + 客户端 ?q= 过滤）。
  4. 页脚 /about /contact /privacy /terms → 404；新增 4 个静态页。
  5. 【SEO】产品页 meta description 缺失（loader 兜底成空串，且 chatgpt.md 无正文无 description）；新增 deriveDescription 兜底 + 给 7 个内容文件补 description frontmatter。
  6. 【SEO】title 双层 `| AooBee`（layout title.template 与 generateMeta 重复拼接）；移除 layout 的 template，title 改为纯字符串。
  7. JSON-LD publisher.logo 指向缺失的 /logo.png → 404；改为 /logo.svg 并新增 public/logo.svg。
  8. schema.ts 新增 normUrl 统一 canonical 末尾斜杠；sitemap.ts URL 补末尾斜杠，与 trailingSlash 一致。
- 验证：BUILD_EXIT=0（26 路由）；重跑审计 0 死链/0 缺失资源/全部页有 title-h1-description/JSON-LD 全部合法；所有标题恰好 1 个 AooBee；0 处 /logo.png 引用。
- 误报澄清：`CChatGPT` 是产品卡片无 logo 时首字母"C"+名称"ChatGPT"拼接，非 bug。

## 使用教程 + 数据录入测试（用户："写出使用教程，下一步本地数据录入测试"）
- 新增 `docs/使用教程.md`：覆盖 content/ 目录结构、各类 frontmatter 字段（分类/产品/文章5类型/点评）、手动/脚手架/AI 三种录入方式、本地构建预览、部署(nginx+Cloudflare)、FAQ。旧 `docs/手动修改内容教程.md` 顶部加废弃指引（已非数据库模型）。
- 数据录入测试：新增真实产品 `content/products/ai/claude.md`，完整走 写md→npm run build→验证。结果：27 路由（原26+1）、llms.txt 9 条（原8）、sitemap 收录 /ai/claude/、分类页 /ai/ 列出、产品页带 SoftwareApplication JSON-LD、title/description 正常。重跑审计 0 死链/0 缺失/全 meta 完整。
- 【发现并修复的 bug】`scripts/content-template.ts` 把产品生成到 `content/products/<slug>.md`（平铺），但 loader 约定产品必须在 `content/products/<分类>/<slug>.md`（子目录），平铺文件不会被加载。已改为按 frontmatter 的 `category` 归到对应子目录。Claude 测试项保留为真实内容。
- 全部本地提交（d138840），未推送（沿用手动批量 push 约定）。预览服务重启在 http://127.0.0.1:4173。

## 新增 7 个分类及测试数据（用户："增加 装修 维修 家电 办公 养老 教育 育儿 这几个分类 并针对这几个分类生成少量测试数据"）
- 新增 7 个分类（order 2-8，icon emoji）：装修(zhuangxiu)、维修(weixiu)、家电(jiadian)、办公(bangong)、养老(yanglao)、教育(jiaoyu)、育儿(yuer)。文件在 `content/categories/<slug>.md`。
- 每个分类各 1 个产品测试数据（放 `content/products/<分类slug>/`，category 字段对齐）：土巴兔/58到家/海尔/钉钉/泰康之家/猿辅导/亲宝宝。
- 每个分类各 1 篇 GUIDE 指南 + 1 篇 FAQ 文章（category 对齐）；分类页 FAQ 区块由该分类的 FAQ 文章驱动（loader 按 category.slug 过滤）。
- 共新增 28 个 .md（content 文件 9→37）；全新构建 BUILD_EXIT=0，55 静态页（原27→55）；审计 77 页 0 死链 0 缺失资源 JSON-LD 合法；sitemap 新增 14 条(7分类+7产品)，llms.txt 9→37 条。
- 已本地提交 63f7030（未推送）。预览服务重启在 http://127.0.0.1:4173，新分类页/产品页均可访问。

## 二次前端审计 + 3 个 bug 修复（用户："自行模拟浏览 检查网站存在的问题和bug"）
- 写了更深的爬虫 `scripts/audit_v2.py`：爬全站 82 页，查死链/缺失资源/JSON-LD/title-h1-description/渲染错误文本/原始 markdown 泄漏/title 重复 AooBee。结果：0 HIGH、0 LOW，仅 2 个 mailto: 误报（非真 bug）。
- 发现并修复 3 个真实 bug（已提交 2c9ef45）：
  1. 【显示】首页分类卡片 + /categories 全显示「0 个产品」—— `parseCategories()` 硬编码 `productCount:0`。新增 `countProductsByCategory()`（只读 frontmatter 的 category 避免触发 getProducts 递归）在 `getCategories()` 里填充真实数（ai=2，其余分类=1）。
  2. 【渲染】首页「热门产品推荐」区静态导出后残留「加载中...」占位—— `<Suspense>` 包裹了一个同步的 `ProductList` 组件，静态导出下 fallback 不解析。移除无意义的 Suspense 包裹（搜索页的 Suspense 保留，因其包客户端 useSearchParams 组件，属必需）。
  3. 【SEO】FAQ 页 meta description 仅 6-10 字（退化成标题）—— FAQ 正文在 faqItems 不在 body，deriveDescription 取不到。新增 `deriveArticleDesc()`：优先用 faqItems 前 3 条问答拼接，否则回退正文首段。
- 验证：重建 BUILD_EXIT=0（55 页）；首页 加载中=0、分类卡片显示真实数、7 个 FAQ 页 description 现 80-160 字真实内容、FAQ 页 JSON-LD 含 Article+FAQPage+BreadcrumbList；所有 .js/.woff2/.css/logo.svg/favicon 资源 200；guide/compare/best 页渲染正常。
- 坑：重构建时旧 `next build`（BOPwEN 后台任务，PID 4222768）卡住并持有 `.next/lock`，导致新构建报 "Another next build process is already running"。用 `taskkill /F /PID 4222768`（注意 taskkill 用单斜杠）杀掉并 `rm -f .next/lock` 后重建成功。PowerShell 里 `$pid` 是只读自动变量，不能赋值，要用别的变量名。
- 全部本地提交（2c9ef45），未推送。预览服务在 http://127.0.0.1:4173。

## 项目清理：删除数据库时代遗留文件 + 依赖 + 字体本地化（用户："检查整个项目 将没必要的文件删除"）
- 验证项目已彻底 md 化：grep 确认 src 业务代码不再 import prisma/DB（仅 src/generated/prisma 自动生成代码里有注释残留）。`package.json` 的 build/gen 脚本也都与 DB 无关。
- 删除的多余文件（均属 DB 时代遗留，md 化后废弃）：
  - 目录：legacy/（旧 admin/api/db 版）、prisma/（schema+migrations）、src/generated/prisma/（自动生成，业务不再引用）
  - 配置：docker-compose.yml（Postgres 容器）、prisma.config.ts
  - 数据：local_data.sql（311KB 旧全量导出）+ 8 个 seed-local-*.sql
  - 旧 DB 脚本：scripts/batch-generate.ts、generate-content.ts、seed-articles.ts、seed-quick.ts、seed.ts、batch-config.json（注意 scripts/generate.ts、generate-batch.ts、gen-llms.ts、content-template.ts 是写 MD 的当前工具，保留）
  - 临时：scripts/audit_v2.py（审计脚本，已无用）、tsconfig.tsbuildinfo（TS 增量缓存，构建自动重建）
  - 过时文档：docs/教程.md、docs/升级说明.md、docs/手动修改内容教程.md（已被 docs/使用教程.md 取代；docs/md化重构方案.md 作为决策记录保留）
- 卸载未用依赖：`npm uninstall @prisma/client @prisma/adapter-pg prisma pg @types/pg`（package.json dependencies 现只剩 next/react/openai/gray-matter/clsx/date-fns/lucide-react/zod）。
- 【附带修复】`rm -rf .next` 后重构建报 `Failed to fetch Geist from Google Fonts`——当前沙箱无法访问 Google Fonts 下载字体。把 `layout.tsx` 的 `next/font/google` Geist 依赖移除，改为在 `globals.css :root` 定义 `--font-geist-sans/mono` 系统字体栈，构建彻底脱离外部网络。验证：HTML 中 google fonts 引用数=0。
- 验证：tsc --noEmit 通过（TSC_EXIT=0，无悬挂 prisma 引用）；BUILD_EXIT=0（55 页）；预览 8 个关键页均 200。
- 全部本地提交（c943abc），未推送。预览服务在 http://127.0.0.1:4173。
- 备注：被删的 tracked 文件（legacy/prisma/seed*.sql/local_data.sql/scripts 旧脚本/docs 旧文档）随时可从 git 历史恢复；src/generated/prisma 与 tsconfig.tsbuildinfo 是自动生成，可重建。

## 二次前端审计与优化（用户："再继续模拟从前端浏览，进行优化与排查"）
- 重写审计脚本 scripts/audit_v3.py（读 out/ 本地导出，检查死链/渲染错误文本/Markdown 泄漏/缺失 meta/正文过短/JSON-LD 覆盖/重复 description）。审计结论：全站 53 页整体健康，0 死链、0 渲染错误、0 Markdown 泄漏、0 缺失 meta、无正文过短页。
- 本轮修复/优化（已提交，squash 为单 commit）：
  1. 【可见 bug】搜索页 `/search` 静态导出下 Suspense fallback 残留『加载中...』且初始无内容。新增 `src/components/search/search-list.tsx` 共享列表组件；`search-results.tsx` 改为无关键词时展示全部条目；`search/page.tsx` 的 Suspense fallback 改为「搜索框+全部条目列表」（SSR 直接渲染 24 条，无『加载中』）。验证：SSR 加载中=0、含全部条目链接。
  2. 【内容质量】法务/关于页 about/contact/privacy/terms 末尾有『占位模板』提示句（被审计命中）。已移除提示句；`legal-page.tsx` 新增 `url` prop 与 BreadcrumbList JSON-LD（四页现均有结构化数据）。
  3. 【SEO】首页与 404 页重复默认 meta description。新增 `src/app/not-found.tsx`：404 设 `robots: noindex` + 独立描述，首页描述不再重复。验证：404 含 `<meta name="robots" content="noindex">`、独立 title。
- 验证：BUILD_EXIT=0（53 页）；JSON-LD 覆盖 50/53（仅 404 三变体除外，且已 noindex）；审计渲染错误文本=0、占位=0。
- 注意：本次把临时审计脚本也误提交进 commit，随后用 `git reset --soft HEAD~2` 将『优化 commit』与『删脚本 commit』squash 为单个干净 commit（净效果=优化，不含审计脚本）。
- 全部本地提交（未推送）。预览服务在 http://127.0.0.1:4173。

## 项目体积分析（用户："项目 800多M 的原因"）
- 结论：项目全量 ~655–860 MB，其中 **node_modules 占 627 MB（约 96%）**；纯源码+内容+配置仅 **1 MB**。`.next` 20M、`.git` 2M、`.workbuddy` 1M、out 7M 皆小。所以"大"全在 npm 依赖，代码本身极小。
- 上一轮 `npm uninstall` 因构建失败中断，导致物理文件残留：**node_modules/@prisma（181M）+ @electric-sql（25M）+ pg/@types/pg** 仍占 206M，但 package.json 与 package-lock.json 已无声明（grep lock=0）。
- 【坑】此时 `npm prune` 说 "up to date" 却**不删这些孤儿物理目录**（npm 本地怪象：lock 已无该包却保留物理目录）。解决：手动 `rm -rf node_modules/@prisma node_modules/@electric-sql node_modules/pg node_modules/@types/pg`，node_modules 832→627 MB，无 git 改动（node_modules 不跟踪）。
- 剩余 627M 大头是框架/工具链固有体积，不可安全删：next 164 + @next 131（含 swc 编译器）、lucide-react 36、typescript 23、@img 20（next 图片）、date-fns 19、openai 14、@esbuild 12、lightningcss 10、@babel 10。devDependencies（ts/eslint/tailwind/postcss）因 build 需要也不能省。
- `npm ls --depth=0` 标记的 extraneous（@emnapi/*、@napi-rs/wasm-runtime、@tybys/wasm-util）是 @img 原生运行时依赖，**保留**，删会破坏 next 图片优化。

## 部署架构对比分析（用户问：原 Vercel+Supabase 工作流是否改变 / GitHub 仓库多余内容）
- 远程仓库：`origin = https://github.com/aoocar/LLM-GEO.git`（GitHub，Vercel 连接的正是它）。项目目录名 aoobee，但 GitHub 仓库名是 LLM-GEO。
- **工作流外壳没变，内核变了**：push → Vercel 自动构建部署的形式仍在；但原架构是「动态全栈 SSR + 运行时连远程 Supabase/Postgres」，现因 md 化 + `output:'export'`，Vercel 构建出纯静态 HTML（out/），运行时**完全不需要数据库**，数据库连接环节整体消失。
- **推送内容已改变**：原仓库大量是 DB 时代文件（Prisma schema/migrations、docker-compose、seed SQL、admin 后台 legacy/、Supabase 连接、/api 写库路由）。这些本地已删且提交（git 跟踪里无残留，当前 107 文件全是新架构：src44/content37/public7/scripts4/docs2+配置）。下次 push 会同步删掉 GitHub 远端的旧 DB 文件。
- **内容生产流程变了**：原靠 /admin 后台写库；现编辑 `content/*.md` + 本地 `npm run gen` 生成，再 push 触发 Vercel 重构建。Vercel 运行时只需静态文件，不再需 DATABASE_URL；仅 `npm run gen` 需 OPENAI_API_KEY（本地 .env 跑，不进仓库）。
- **安全确认**：`.gitignore` 第34行 `.env*` 已忽略 `.env`，`git ls-files` 确认 `.env` 未跟踪 → push 不会带本地 DATABASE_URL/OPENAI_API_KEY，无泄露风险。仓库无 `.github/`/`.vercel/`/supabase 配置 → Vercel 零配置自动检测。
- **仍需处理：README.md 过时**（最该改）：仍写「基于 Next.js、Prisma 与 PostgreSQL」「docker compose up 启动数据库」「/admin 管理后台」「npx prisma studio」，并链接到**已删除**的 docs/教程.md、docs/升级说明.md（死链）。待更新为 Markdown 内容源 + 静态导出 + gen 流程。
- **Vercel 控制台侧**（需用户手动，无凭证我不做）：若 Vercel 项目还配着 Supabase/DATABASE_URL 等 env，现在用不到，可在 Vercel 网页清掉。
- 已更新 README.md 为新架构（commit 3203ee2，本地），并 present 给用户。远程默认分支是 **`main`**（不是 master），push 用 `git push origin main`。

## git push 阻塞（2026-07-23 下午）
- 本地 8 个 commit 全部就绪（b6c3946 → 3203ee2），待推 `origin/main`。
- **本沙箱无法 push**：git credential.helper=`helper-selector`，无头环境弹不出登录框而挂起；`GIT_TERMINAL_PROMPT=0` 实测报错 `could not read Username for 'https://github.com'`（无缓存凭证）。无 GitHub token 环境变量、无 SSH remote。
- 结论：需用户在自己有 GitHub 凭证的终端跑 `git push origin main`，或提供 PAT 让我用内联 URL 推（不写进 stored remote）。
- 用户规划：先 GitHub+Vercel 免费部署；等完善或免费额度耗尽后迁到自有云服务器（自托管基本=把 `npm run build` 产出的 `out/` 内容上传到域名 web 根目录，配合 nginx `try_files $uri $uri/ =404;` 因 trailingSlash 用目录+index.html）。
- 【已完成】用户于本机（有 GitHub 凭证）执行 `git push origin main`，8 个新架构 commit（b6c3946→3203ee2）已推上 `github.com/aoocar/LLM-GEO`。沙箱无凭证未参与推送。后续：用户在 Vercel 确认构建成功 + 手动清理 Supabase/DATABASE_URL 等无用 env。
- 【Vercel 部署被拦】push 成功后 Vercel 报错 "Deployment Blocked: commit email shirenzu@example.com could not be matched to a GitHub account"。根因：git 全局 user.email=`shirenzu@example.com`、user.name=`拾人族` 是占位身份，不匹配 GitHub 账号，触发 Vercel 部署保护（要求 commit author 邮箱属于仓库协作者的已验证 GitHub 邮箱）。修复两条路：① 把 8 个 commit 的作者邮箱改成用户真实 GitHub 邮箱后 `git push --force-with-lease`（需用户给真实邮箱）；② Vercel 项目 Settings→Git 关掉 "Prevent deployments from commits authored by unknown email addresses" 保护后重部署（不改 git）。待用户选择。
- 【已选方案A+已执行】用户给真实邮箱 aoocar@gmail.com、域名 www.aoobee.com。本地 git 仓库身份改为 aoocar/aoocar@gmail.com；执行 `GIT_SEQUENCE_EDITOR=true git rebase HEAD~8 --exec "git commit --amend --no-edit --reset-author"`，将 8 个新架构 commit（原 b6c3946→3203ee2，现 3031200→def6d75）作者邮箱改写为 aoocar@gmail.com，提交信息不变。更早的 cec7eb9/初始 commit 仍为占位邮箱（Vercel 只校验 HEAD commit 作者，无影响）。stash 的内存笔记已 pop 恢复。待用户在有权限的终端执行 `git push --force-with-lease origin main` 强推，触发 Vercel 重部署。

## Vercel 环境变量 NEXT_PUBLIC_SITE_URL（用户问是否保留）
- **结论：保留，且值应设为 `https://www.aoobee.com`**。它是站点绝对地址唯一来源，被 6 处使用：layout.tsx 的 `metadataBase`（派生 canonical/OG/Twitter 绝对 URL）、seo/meta.ts 的 canonical/og、seo/schema.ts 的 JSON-LD、sitemap.ts、robots.ts、scripts/gen-llms.ts 的 llms.txt。每处都有 fallback `|| "https://www.aoobee.com"`。
- 若 Vercel 里该值已是 `https://www.aoobee.com` → 留着即可；若填的是 localhost/旧域名/http → 必须改成 `https://www.aoobee.com`，否则 canonical/sitemap/JSON-LD 会生成错误绝对地址（SEO 受损）。
- 因是 `NEXT_PUBLIC_*`，构建时内联进包，**改值需重新部署（rebuild）才生效**。
- 与 Supabase/DATABASE_URL 不同，这是 md 化后仍需保留的少数 env 之一（后者应删）。

## 线上部署审计（用户发来 https://www.aoobee.com/）
- Vercel 部署已成功（force-push 改写邮箱后放行），线上站可达。
- **抽查端点全绿**：robots.txt（含 Sitemap 声明）、sitemap.xml（根域 https://www.aoobee.com，覆盖分类/产品/文章/指南/FAQ/对比/点评等 50+ URL）、llms.txt（37 条内容清单）均正常。
- **发现 1 个真实 SEO 缺陷（已修，commit 946094e）**：首页缺 `<link rel="canonical">` 与 `og:url`。根因：首页 `page.tsx` 一直只继承 `layout.tsx` 的静态 metadata，而 layout 没设 `alternates.canonical`/`openGraph.url`；其余子页面（/ai、/about、/privacy 等）因都调了 `generateMeta()` 正常带 canonical+og:url。修复：`page.tsx` 增加 `export const metadata = generateMeta({title, description})`（url 省略→根域），首页 canonical/og:url 现为 https://www.aoobee.com。
- **顺手修正夸大描述**：layout.tsx 全局 description 原写「覆盖 50+ 行业，收录 5000+ 产品」（实际 8 分类 9 产品），改为真实覆盖领域表述。
- 验证方式：curl 抓线上原始 HTML，grep canonical/og:url/title/JSON-LD（WebFetch 转 markdown 会丢 head，必须用 curl 看原始标签）。
- 本地 `npm run build` 因沙箱 CPU 极慢（35 分钟未出）已手动停掉；改动是教科书式 `export const metadata = generateMeta(...)`，与 20+ 子页面同款写法且已部署成功，不可能引发构建错误，故直接提交，由 Vercel 构建验证。已本地提交 946094e，待用户 `git push origin main` 触发线上修复。
- 备注：`public/llms.txt` 仅有 LF/CRLF 换行差异（git autocrlf 误报），已 `git checkout` 还原，未提交噪声；`.obsidian/` 是本地 Obsidian 配置，未跟踪不提交；`.next/`、`out/` 已在 .gitignore。

## 线上复检（push 后，用户确认已执行 git push origin main）
- 用户推送后复检线上 https://www.aoobee.com/：**修复已生效**。响应头 HTTP 200、`Content-Type: text/html; charset=utf-8`、`Server: Vercel`、`Last-Modified: Thu, 23 Jul 2026 07:25:25 GMT`（新构建时间）、`X-Vercel-Cache: HIT`。
- Python 以 UTF-8 严格解析确认：首页 `<link rel="canonical" href="https://www.aoobee.com/">` 存在、`og:url=https://www.aoobee.com/` 存在、description 为真实表述且无 U+FFFD 替换符（编码干净）。
- 澄清：此前在 GBK 语言环境 shell 用 `cut -c1-120` 截断多字节 UTF-8 字符串显示的"乱码"（鏄�鍏ㄨ�屼笟…）纯属终端显示假象，部署的实际 HTML 是合法 UTF-8，并非真编码 bug。
- 结论：线上 SEO 标签（canonical/og:url/description/JSON-LD/sitemap/robots/llms.txt）现全部正确，无待办。

## 接入网站分析（用户问 Clarity + Google Analytics 如何配置）
- 用户给了 GA4 代码，measurement ID = `G-LFP31YGTRT`（gtag.js 片段）。
- 做法：新增 `src/components/analytics.tsx`，用 `next/script`（`strategy="afterInteractive"`）注入 GA + Clarity，根布局 `layout.tsx` 渲染 `<Analytics />`。该组件可被 Server Component 直接使用（next/script 内部已处理客户端注入），兼容 `output:'export'` 静态导出（Vercel 与后续 nginx 自托管均可用）。
- GA：默认 ID=`G-LFP31YGTRT`，可被环境变量 `NEXT_PUBLIC_GA_ID` 覆盖（构建时内联）。
- Clarity：由 `NEXT_PUBLIC_CLARITY_ID` 驱动，仅在配置时注入（未配置不渲染，不影响站点）。用户尚未提供 Clarity project ID，待其从 clarity.microsoft.com 项目设置里取「Setup tag」里的 ID 后：① 直接硬编码为 fallback，或 ② 在 Vercel 设 `NEXT_PUBLIC_CLARITY_ID` 环境变量。
- 已 `tsc --noEmit` 通过（exit 0）；本地未跑全量 `next build`（沙箱 CPU 极慢，约 35 分钟）。改动为标准 next/script 用法，风险低，由 Vercel 构建验证。
- 注意：`NEXT_PUBLIC_*` 在构建时内联，改值/新增 env 需重新部署（redeploy）才生效。
- 已本地提交 74f6638，待用户 `git push origin main` 上线。

## Clarity project ID 收尾（用户提供）
- 用户给出 Clarity project ID = `lx18ztcr4b`。更新 `src/components/analytics.tsx`：`CLARITY_ID` 加默认值 `lx18ztcr4b`（可被 `NEXT_PUBLIC_CLARITY_ID` 覆盖），移除原「仅配置时注入」的条件分支，改为恒注入（与 GA 同款行为）。`tsc --noEmit` 通过（exit 0）。已本地提交 befbe1a，待 `git push origin main` 上线。此后 GA 与 Clarity 均带默认 ID，不配 Vercel env 也能跑。



## 线上复检 analytics + 验证教训
- 用户推送三次（def6d75->0d8ef51->c176864 全成功），并本地 `npm run build` 成功（14.4s，55 页）。
- 关键验证教训(重要，避免再误判)：
  1. next/script 的 afterInteractive 在 output:'export' 静态导出下确实会把脚本写进预渲染 HTML(嵌在 RSC 序列化载荷里，由 Next 客户端运行时 hydrate 后注入执行)，GA 甚至会生成 <link rel="preload">。analytics 集成有效。
  2. Clarity 官方 snippet 的 URL 是 JS 拼接的：t.src="https://www.clarity.ms/tag/"+i(i="lx18ztcr4b")，字面量 clarity.ms/tag/lx18ztcr4b 永不出现在 HTML 中——用该字面量 grep/curl 必得 FALSE NEGATIVE。正确验证 token：ms-clarity / lx18ztcr4b / clarity.ms/tag / G-LFP31YGTRT / googletagmanager.com/gtag/js?id=G-LFP31YGTRT。
  3. Vercel 部署列表可能滞后(用户贴的列表只到 0d8ef51，实际已构建 c176864)。确认新构建是否上线看响应头 Last-Modified 是否更新(旧 07:25:25 -> 新 08:02:01)，而非只看部署列表。
- 最终结论：live 站 Last-Modified: 08:02:01 GMT、X-Vercel-Cache: MISS，GA/Clarity 全部 token 命中 -> analytics 集成已生效。

## 线上全站审计（用户："模拟流量检查一遍 优化/bug"）
- 方法：抓 sitemap.xml(42 URL) -> 审计 39 个 HTML 页 + 批量校验 60 条站内链接 + 70 个静态资源。脚本 scripts/_audit_live.py 为临时，已删。
- 正确性结论(全绿)：0 死链(4xx/5xx)、0 坏资源、0 非200、0 缺 meta/canonical/jsonld/h1、0 canonical 错配、无 Markdown 泄漏、无 JSON-LD 解析错误。
- 误报排查："undefined" 命中全部 39 页，但剥离 <script> 后验证：首页 38 处 undefined 全在 RSC 序列化载荷内、0 处在可见文本 -> 非 bug(Next 框架序列化数据)。**教训：以后审计 error pattern 必须只对"可见文本"(strip script/style 后)判断，否则会误报框架数据。**
- 优化缺口(按价值排序)：
  1. og:image 缺失 -> meta.ts 仅当传入 image 才生成 images，无页面传 image，故全站无社交分享预览图。修复方向：generateMeta 加默认图(public/og-default.png) 或 用 opengraph-image.tsx 动态生成每页图(品牌化，构建期产出静态 PNG，兼容 output:export)。
  2. 安全响应头仅 HSTS；缺 Referrer-Policy、X-Content-Type-Options(低风险易加)；CSP 因内联 analytics 脚本需谨慎(允许 unsafe-inline)。注意：静态导出下 next.config headers() 不生效，需 vercel.json 或 Vercel 控制台设。
  3. 静态资源缓存已最优：/_next/static Cache-Control: public,max-age=31536000,immutable。
  4. 全站 0 个 <img>(用 SVG/图标)，无图片懒加载/CLS 隐患。
- 次要：Clarity 未 preconnect(仅 GA gtag 被 preload)，可加 <link rel=preconnect href=https://www.clarity.ms> 小幅提速。

## OG 图 + 安全头 + preconnect 实现（commit 98181b7）
- 三件事完成：A) 每页动态品牌 OG 图（next/og）；B) vercel.json 安全响应头；C) layout 加 Clarity/gtag preconnect。
- OG 实现：src/components/og/og-image.tsx（buildOgImage，ImageResponse 1200x630，品牌渐变+标题+eyebrow）；9 个 opengraph-image.tsx（根 + (directory)/[category]、[category]/[slug]、(content)/best/[keyword]、compare/[slug]、faq/[slug]、guide/[slug]、review/[slug]、reviews/[slug]），每个带 generateStaticParams（与对应 page 一致，静态导出必需）。标题取自 loader：分类名/产品名/getArticle().title/getReview().title。
- 中文字体坑（关键，已解决）：next/og 默认字体不含中文；Vercel=Linux 无系统中文字体 → 字体必须提交仓库。且 ImageResponse bundle 限制 500KB、satori 不能解析可变字体 fvar（直接崩溃 TypeError）。最终方案：系统 Noto Sans SC 可变字体 → fontTools.varLib.instancer 实例化为静态 Regular(wght=400) → pyftsubset 按 content/ 用字+标点子集化 → assets/fonts/NotoSansSC.ttf（273KB，无 fvar/gvar）。
- 复现/扩充字体子集命令（python 用托管：C:/Users/aoobe/.workbuddy/binaries/python/versions/3.13.12/python.exe -m fontTools...；fonttools 已装）：
  cp "/c/Windows/Fonts/NotoSansSC-VF.ttf" assets/fonts/_src.ttf
  python -m fontTools.varLib.instancer assets/fonts/_src.ttf wght=400 -o assets/fonts/_static.ttf
  python -m fontTools.subset assets/fonts/_static.ttf --text-file=assets/fonts/_og_chars.txt --output-file=assets/fonts/NotoSansSC.ttf --no-hinting
  rm -f assets/fonts/_src.ttf assets/fonts/_static.ttf
  （_og_chars.txt = 提取 content/*.md 用字 + 品牌串 + ASCII + 标点范围；python 写脚本生成）
- 验证：tsc 通过；用真实 buildOgImage 生成含中文 PNG 成功（1200x630 valid PNG，satori 不再 fvar 崩溃）。
- 维护 caveat：字体子集仅含「当前站点内容」用字。若以后新增含全新汉字的内容，需重跑上面子集命令（否则该字在 OG 图显示空白框，不会中断构建）。这是静态导出下「中文字体必须进仓库」的固有取舍。
- vercel.json 头仅对 Vercel 生效；自托管(nginx)需另行配置。CSP 未加（analytics 内联脚本需 unsafe-inline，暂不加）。
- 尚未推送；用户本地 npm run build + git push origin main 后上线，上线后复检 og:image 与响应头。
